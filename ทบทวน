การเรียกใช้ภาพจาก unsplash ต้องใช้อันนี้
imgElement.src =  `https://source.unsplash.com/225x225/?${keyword}&sig=${index}`;

เชื่อมกับสคลิป
<script src="index.js"></script>

การเรียกใช้ฟังก์ชัน
ชื่อฟังก์ชัน();
main();

ประกาศฟังก์ชัน
function ชื่อฟังก์ชัน(){
}
function main(){
    
}

การใชลูปfor
for(let ชื่อตัวแปล = ค่า; สิ่งที่จะให้ฟังก์ชันทำ){
    ค่าเป็น ture ให้ทำอะไร
}
for(let i = 1; i<=12; i++){
            appendImageElement(keyword, i);
}